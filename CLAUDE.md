# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is newly initialized and currently empty. No build system, language, or framework has been committed yet. Update this file once the project structure is established.

## Intended Purpose

Based on the repository name "Agent Harness", this project is likely a framework or runtime for orchestrating AI agents. When the codebase takes shape, document:

- The primary language and runtime (e.g., Python, Node.js, Go)
- How to install dependencies
- How to build, run, and test
- The agent lifecycle (registration, invocation, teardown)
- How agents communicate (message passing, shared state, etc.)
- Configuration and environment variables
